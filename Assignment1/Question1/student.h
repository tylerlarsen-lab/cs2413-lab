#ifndef QUESTION1_STUDENT_H
#define QUESTION1_STUDENT_H

/*
 * Q1: Merge Sorted Array
 *
 * Merge nums2 into nums1 as one sorted array.
 * nums1 has length nums1Size == m + n.
 * The first m elements of nums1 are valid; the last n are placeholders (0).
 * nums2 has length nums2Size == n.
 *
 * Store the result in nums1 (in-place). Do NOT return anything.
 *
 * Suggested signature (C-friendly for testing):
 */
void merge(int* nums1, int nums1Size, int m, int* nums2, int nums2Size, int n);

#endif
